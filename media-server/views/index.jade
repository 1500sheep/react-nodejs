extends layout

block content
  h1= title
  form(action='/sound/upload', method='post', enctype='multipart/form-data')

    p file :
      input(type='file', name='myfile1')
      input(type='submit', value='Upload')
